import Colors from './Colors'

const ApplicationStyles = {
  screen: {
    mainContainer: {
      flex: 1,
      backgroundColor: Colors.white
    },
    header: {
      height: 60,
      width: '100%',
      flexDirection: 'row',
      borderWidth: 1,
      borderColor: Colors.pinkIOS,
      borderRadius: 10
    }
  }
}

export default ApplicationStyles
